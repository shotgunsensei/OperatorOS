# `@operatoros/ui` Placeholder

Reserved package boundary for future shared UI primitives.

No package manifest or runtime code is added in Phase 1. Current production UI
remains in `apps/web/src`.

Future extraction must preserve product-specific visual direction and avoid
moving module business logic into generic UI components.
