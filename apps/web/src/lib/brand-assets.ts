/**
 * Canonical OperatorOS visual assets.
 *
 * Keep consumers on these paths instead of scattering string literals across
 * metadata, shared chrome, and marketing surfaces. Compact contexts use the
 * mark only; the full lockup is reserved for layouts where the wordmark and
 * domain remain readable.
 */
export const OPERATOROS_MARK_PATH = '/brand/operatoros-mark.png';
export const OPERATOROS_LOGO_PATH = '/brand/operatoros-logo.png';
export const OPERATOROS_ASSET_SIZE = 1254;
