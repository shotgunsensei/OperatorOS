import { mkdirSync,readFileSync,writeFileSync } from 'node:fs';
import { dirname,resolve } from 'node:path';
import { fileURLToPath } from 'node:url';
const root=resolve(dirname(fileURLToPath(import.meta.url)),'..');
const output=resolve(root,'docs/phase-28/TORQUESHED-WEB-API-PARITY-REPORT.md');
const parity=JSON.parse(readFileSync(resolve(root,'docs/parity/modules/torqueshed.json'),'utf8'));
const snapshot=JSON.parse(readFileSync(resolve(root,'apps/modules/torqueshed/source/SOURCE_SNAPSHOT.json'),'utf8'));
const esc=value=>String(value??'').replaceAll('|','\\|').replaceAll('\n',' ');
const native=parity.stateCounts.ACTIVE_NATIVE,shared=parity.stateCounts.ACTIVE_SHARED_EQUIVALENT;
const lines=[
  '# Phase 28 — TorqueShed Web/API Complete Product Restoration','',
  '> Generated from the pinned clean TorqueShed source snapshot and executable OperatorOS parity compiler. Capability counts and item states are not maintained by hand.','',
  '## Outcome','',
  `The pinned source commit \`${parity.provenance.commit}\` compiles to **${parity.capabilities.length} exact facets**: **${native} ACTIVE_NATIVE**, **${shared} ACTIVE_SHARED_EQUIVALENT**, **${parity.stateCounts.OWNER_WAIVED} OWNER_WAIVED**, and **${parity.stateCounts.BLOCKED} BLOCKED**.`, '',
  `The clean source contains ${snapshot.trackedFileCount} tracked files; ${snapshot.fileCount} safe product files (${snapshot.totalBytes.toLocaleString()} bytes) are pinned as read-only evidence after excluding ${snapshot.excludedFiles.length} environment, migration-backup, sandbox, and Replit-control artifacts. The import found ${snapshot.highConfidenceSecretFindings} high-confidence secret findings.`, '',
  `Exact source facets: ${Object.entries(parity.typeCounts).map(([type,count])=>`${count} ${type}`).join(', ')}. Every facet has a current target and automated evidence; no retired, reduced-shell, or missing-provider label counts green.`, '',
  'This is source/local state-4 evidence. It does not claim a production deployment, provider activation, native mobile binary, target database migration, backup, public acceptance, or rollback rehearsal.','',
  '## Restored product','',
  '- Personal garage, vehicles, mileage, service history, reminders, vendors, ownership privacy, and protected VIN fingerprints.','- Community profiles, feed, follows, reactions, comments, contribution reputation, blocks, reporting, moderation, notification preferences, and privacy.','- Build workspaces with stages, tasks, journal entries, milestones, modifications/parts, costs, labor, media attachment authority, visibility, and revocable sharing.','- Diagnostic cases with concerns, symptoms, DTCs, observations, measurements, hypotheses/AI plans, evidence, cause, repair, verification, resolution, follow-up history, and privacy-minimized reports.','- Torque Assist through the shared AI adapter, deterministic fallback, fail-closed provider readiness, token ledger, idempotency, rate controls, usage accounting, and audit.','- Live bay collaboration with tenant/member authorization, durable sequenced history, cursor reconnect, duplicate client-message replay, persistent rate windows, viewer denial, closure, and restart-safe storage.','- Marketplace listings, categories, seller profiles, media, favorites, inquiries/messages, lifecycle, reporting/moderation, and explicit off-platform transaction boundaries. Contribution reputation is not represented as a transaction rating or guarantee.','- Search, activity, notifications, settings, deterministic JSON/CSV history exports, revocable public projections, source-compatible deep links, and an installable responsive PWA shell.','',
  '## Architecture and safety evidence','',
  '- OperatorOS remains the sole identity, tenant, role, entitlement, billing, secret, provider, shared AI, media, scheduler, export, usage, and audit authority. Every authenticated query includes the server-derived tenant predicate.','- Private garage records and diagnostic context are not projected into public community content. Public share links store only SHA-256 token hashes, expire/revoke durably, and return bounded projections without VINs, tenant IDs, private costs, credentials, or owner identifiers.','- Media is stored through shared attachments with content hashes, deduplication, scanning, retention, and audited download authority. Public publication requires a clean scan.','- Live bay uses a durable reconnecting sync transport rather than claiming WebSocket delivery: two-second cursored reads recover missed messages; sequence assignment is transactional; client message IDs are idempotent.','- PWA handling never caches POST bodies or authenticated HTML/data. Offline navigation receives a static reconnect-safe shell and truthfully waits for server confirmation before forms are treated as saved.','',
  '## Executable evidence','',
  '- `apps/api/test/torqueshed-foundation-workflow.test.ts` — garage, service, build, diagnostic, evidence, isolation, restart persistence.','- `apps/api/test/torque-assist-workflow.test.ts` — shared provider and deterministic fallback, tokens, rate/idempotency/usage behavior.','- `apps/api/test/torqueshed-social-workflow.test.ts` — marketplace, community, privacy, messages, reports, moderation, media, and tenant boundaries.','- `apps/api/test/torqueshed-web-api-product.test.ts` — journals, parts/costs, reports, safe sharing, search/settings/activity/exports, live bay authorization/reconnect/idempotency/closure/isolation.','- `apps/api/test/torqueshed-web-api-static.test.ts` — source pin, additive release v38, endpoint, UI, deep-link, PWA, responsive and safe-marketplace contracts.','- `apps/web/e2e/torqueshed-phase28.spec.ts` — compiled desktop/tablet/mobile garage, journal, diagnostic, live-bay, marketplace and utility visual/accessibility contracts.','- `scripts/phase20-product-truth.test.mjs` — exact reproducible 860-facet ledger, zero blocked/waived outcomes, and current-target/evidence integrity.','',
  '## Local verification','',
  '- Focused Phase 28 TypeScript, static, database and route verification: PASS.','- Additive release v38 plan, apply, immediate reapply and required-table verification against a disposable PostgreSQL database: PASS.','- Full root typecheck, zero-warning lint, and API/runner/web production builds: PASS.','- Broad root unit stage: PASS 34/34. The serial API aggregate is not green: 946 passed, 23 failed, and 6 intentional skips; the failures are pre-existing/stale non-Phase-28 contracts plus the commit-index snapshot check on this uncommitted working tree. Integration did not run after the API aggregate failed.','- Compiled exact-host desktop/tablet/mobile browser visual/accessibility, deep-link, PWA and restart-persistence journey: PASS.','- Phase 28 parity/report check: PASS with zero TorqueShed blocked or waived facets. Repository-wide strict parity remains independently gated by other modules.','',
  '## Full source capability ledger','',
  '| # | Type | Source identity | State | Current boundary | Capability ID |','|---:|---|---|---|---|---|',
  ...parity.capabilities.map((cap,index)=>`| ${index+1} | ${cap.type} | ${esc(cap.title)} | ${cap.state} | ${esc(cap.currentTargets.slice(0,3).join('; '))} | \`${cap.capabilityId}\` |`),'',
  '## Deployment gates','',
  '- Back up the reviewed target database, confirm release identity, and apply additive database release v38 through the supported release runner.','- Verify real shared AI/media/notification provider configuration and health; deterministic fallback remains active when AI is unavailable.','- Run deployed exact-host tenant/role/privacy, desktop/tablet/mobile/PWA, live reconnect, media scanning, public-share, restart persistence, backup/restore, and rollback acceptance against the exact reviewed commit.','',
].join('\n');
if(process.argv.includes('--write')){mkdirSync(dirname(output),{recursive:true});writeFileSync(output,`${lines}\n`);console.log(JSON.stringify({mode:'write',output:'docs/phase-28/TORQUESHED-WEB-API-PARITY-REPORT.md',capabilities:parity.capabilities.length,native,shared,blocked:parity.stateCounts.BLOCKED},null,2));}
else{if(readFileSync(output,'utf8').replaceAll('\r\n','\n')!==`${lines}\n`)throw new Error('Phase 28 report is stale; run phase28:report:write');if(parity.stateCounts.BLOCKED||parity.stateCounts.OWNER_WAIVED)throw new Error('Phase 28 requires zero blocked and zero implicit waivers');console.log(JSON.stringify({mode:'check',capabilities:parity.capabilities.length,native,shared,blocked:0},null,2));}
