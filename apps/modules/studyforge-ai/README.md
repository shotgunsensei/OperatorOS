# StudyForge AI Module Import

Status: source snapshot imported; partial native runtime exists.

- Canonical host: `https://studyforge-ai.operatoros.net`
- Commercial class: paid add-on
- Source provenance and exclusions: `source/SOURCE_SNAPSHOT.json`

The active shared runtime currently covers a tenant-scoped flashcard-session
slice. The imported source adds study sets, folders, quizzes, progress,
planning, countdowns, templates, account lifecycle, AI, and Stripe-era flows.
Its local passwords/sessions, user-only ownership, and demo instant-upgrade
path are not active. Port remaining workflows into tenant-scoped namespaced
tables while keeping OperatorOS billing and entitlement authority.

