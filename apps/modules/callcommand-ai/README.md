# CallCommand AI Module Import

Status: source snapshot imported; partial native telephony runtime exists.

- Canonical host: `https://callcommand-ai.operatoros.net`
- Commercial class: paid add-on
- Source provenance and exclusions: `source/SOURCE_SNAPSHOT.json`

The shared runtime currently covers test calls, call logs/status, provider
connection, and bounded Twilio callbacks. The imported source includes
channels, flows, receptionist profiles, automation, live sessions, transfers,
tickets, leads, tasks, ingestion, storage, and simulations. Its Clerk user IDs,
standalone session assumptions, Replit admin configuration, and checkout stub
are not active. Port features behind tenant-aware telephony/storage services,
verified Twilio signatures, idempotency, retention, and usage gates.

